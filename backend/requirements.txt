# Dependances installees par Nixpacks au deploiement Railway.
# Doit rester aligne sur pyproject.toml / uv.lock.
#
# L'application parle exclusivement a Supabase via PostgREST : il n'y a ni
# SQLAlchemy, ni Alembic, ni connexion Postgres directe. Les paquets
# correspondants (Flask-SQLAlchemy, Flask-Migrate, Flask-JWT-Extended,
# sqlalchemy, psycopg2-binary, bcrypt, marshmallow) ont ete retires : plus
# aucun module ne les importait.

Flask>=3.0.0
Flask-Cors>=4.0.0
Flask-Limiter>=3.8.0
python-dotenv>=1.0.0
supabase>=2.3.0
pydantic[email]>=2.0.0
requests>=2.31.0
resend>=0.8.0
gunicorn>=21.2.0

# Tests uniquement — inoffensif en production, mais inutile au runtime.
pytest>=8.0.0
